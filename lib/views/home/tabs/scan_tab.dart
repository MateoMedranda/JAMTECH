import 'package:flutter/material.dart';

// Este tab ha sido reemplazado por CobrarTab.
class ScanTab extends StatelessWidget {
  const ScanTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
